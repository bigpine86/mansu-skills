interface:
  display_name: "Mansu TDD Lite"
  short_description: "Plan-first slice workflow with lighter RED-test requirements"
  default_prompt: "Use $mansu-tdd-lite to plan properly, split the work into vertical slices, choose each slice's validation path before coding, then run review, QA, checkpoint, commit, and worklog updates."
