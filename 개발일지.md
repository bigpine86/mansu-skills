# 개발일지

## 2026-04-24: Harden `mansu-tdd-lite`
> **"lite는 가볍게 유지하되, 최소 계약은 모호하지 않게 만들었다."**

### 1. Work summary
- `mansu-tdd-strict`와 `mansu-tdd-lite`를 새 `mansu-skills` 저장소로 가져왔다.
- `mansu-tdd-lite`에 lightweight preflight, compact slice card, minimum slice close, closeout 규칙을 추가했다.
- 구조 계약을 검증하는 `scripts/validate_mansu_tdd_lite.sh`를 추가했다.
- 개선된 `mansu-tdd-lite`를 설치된 로컬 skill 경로에도 동기화했다.

### 2. Slice completed
- Slice 1: repo bootstrap + `mansu-tdd-lite` 최소 계약 보강 + 구조 검증 스크립트 추가

### 3. Verification
- RED: `scripts/validate_mansu_tdd_lite.sh` initial FAIL
- GREEN: `scripts/validate_mansu_tdd_lite.sh` PASS
- Sync check: repo version copied back to installed `mansu-tdd-lite`

### 4. Files
- `mansu-tdd-strict/**`
- `mansu-tdd-lite/SKILL.md`
- `mansu-tdd-lite/agents/openai.yaml`
- `scripts/validate_mansu_tdd_lite.sh`
- `PLAN.md`

### 5. Follow-up
- repository source와 installed skill source를 어떻게 동기화할지 정해야 한다.

## 2026-04-24: Reframe `mansu-tdd-lite`
> **"lite는 대충 하는 모드가 아니라, strict RED 테스트 강제만 완화하는 모드다."**

### 1. Work summary
- `mansu-tdd-lite`의 정체성을 다시 잡았다.
- 계획, vertical slice, review, QA, checkpoint, commit, worklog는 약화하지 않는다고 명시했다.
- 완화되는 부분은 매 slice마다 반드시 failing RED test로 시작해야 한다는 strict TDD 요구뿐이라고 정리했다.
- `agents/openai.yaml`과 lite 검증 스크립트도 새 원칙에 맞게 업데이트했다.

### 2. Verification
- `scripts/validate_mansu_tdd_lite.sh` PASS

### 3. Follow-up
- `mansu-tdd-strict`와 `mansu-tdd-lite`의 차이를 README나 skill map에 짧게 정리할 수 있다.

## 2026-04-24: Add `mansu-tdd-total`
> **"`total`은 super-executor가 아니라, 계획하고 slice별로 lite/strict를 고르는 router다."**

### 1. Work summary
- multiagent 검증으로 `total = planner/router`, `lite/strict = executor mode` 구조를 확인했다.
- `mansu-tdd-total`을 새 skill로 추가했다.
- `total`에 preflight, planning loop, dispatch rules, slice table, execution handoff, mode-change rules, closeout을 넣었다.
- `mansu-tdd-lite`와 `mansu-tdd-strict`에 Mansu TDD series 안에서의 역할을 추가했다.
- `total`과 series 구조를 검증하는 스크립트를 추가했다.
- repo 버전을 installed local skill 경로에도 동기화했다.

### 2. Slice completed
- Slice 1: `mansu-tdd-total` router 추가 + `lite/strict` executor 역할 보강 + validation script 추가

### 3. Verification
- `scripts/validate_mansu_tdd_total.sh` PASS
- `scripts/validate_mansu_tdd_lite.sh` PASS
- `scripts/validate_mansu_tdd_series.sh` PASS
- repo skill files and installed local skill files diff: clean
- `git diff --check` PASS

### 4. Files
- `mansu-tdd-total/SKILL.md`
- `mansu-tdd-total/agents/openai.yaml`
- `mansu-tdd-lite/SKILL.md`
- `mansu-tdd-lite/agents/openai.yaml`
- `mansu-tdd-strict/SKILL.md`
- `scripts/validate_mansu_tdd_total.sh`
- `scripts/validate_mansu_tdd_series.sh`
- `PLAN.md`

### 5. Follow-up
- `mansu-skills` repo를 원격 `main`에 올릴지, 현재 branch를 리뷰용으로 둘지 결정해야 한다.

## 2026-04-24: Lock Mansu TDD invariants
> **"`lite`와 `strict`의 차이는 품질 강도가 아니라 RED 테스트 강제 여부다."**

### 1. Work summary
- 외부 TDD/vertical slicing 자료와 현재 스킬 구조를 대조했다.
- `mansu-tdd-total`에 `Shared invariants` 섹션을 추가했다.
- `lite`는 lower-quality mode가 아니며 planning, slicing, review, QA, checkpoint, commit, work logging은 유지한다고 명시했다.
- `strict`는 meaningful failing RED test가 confidence/design/risk protection을 높이는 slice에 쓰는 모드라고 보강했다.
- validation script가 이 철학 문구를 확인하도록 업데이트했다.

### 2. Verification
- `scripts/validate_mansu_tdd_series.sh` PASS
- repo skill files and installed local skill files diff: clean
- `git diff --check` PASS

### 3. Follow-up
- 필요하면 이 철학을 README 또는 `mansu-tdd-total`의 prompt examples에도 짧게 반영할 수 있다.

## 2026-04-24: Add `mansu-debug-rootcause`
> **"버그는 기능 작업이 아니다. 원인을 확인하기 전에는 수정하지 않는다."**

### 1. Work summary
- 기존 Oh My Codex `debug` 스킬에서 `reproduce -> isolate -> hypothesize -> test -> fix -> verify` 흐름을 참고했다.
- `gstack-investigate`에서 `no fixes without root cause`, 3 hypotheses stop rule, minimal fix, fresh verification, debug report 원칙을 가져왔다.
- `mansu-debug-rootcause`를 새 skill로 추가했다.
- `mansu-tdd-*`와 달리 feature planning이 아니라 root-cause investigation 전용 축으로 분리했다.
- 전체 Mansu skill 검증 스크립트 `scripts/validate_mansu_skills.sh`를 추가했다.
- repo 버전을 installed local skill 경로에도 동기화했다.

### 2. Slice completed
- Slice 1: `mansu-debug-rootcause` 추가 + debug validation script 추가 + installed local skill sync

### 3. Verification
- RED: `scripts/validate_mansu_debug_rootcause.sh` initial FAIL
- GREEN: `scripts/validate_mansu_debug_rootcause.sh` PASS
- `scripts/validate_mansu_skills.sh` PASS
- repo debug skill and installed local debug skill diff: clean

### 4. Files
- `mansu-debug-rootcause/SKILL.md`
- `mansu-debug-rootcause/agents/openai.yaml`
- `scripts/validate_mansu_debug_rootcause.sh`
- `scripts/validate_mansu_skills.sh`
- `PLAN.md`

### 5. Follow-up
- 실제 버그 작업에서 써본 뒤, report format과 escalation 기준을 더 날카롭게 다듬을 수 있다.

## 2026-04-24: Map debug skill sources
> **"`mansu-debug-rootcause`는 독자 발명이 아니라, Oh My와 gstack의 디버그 루틴을 Mansu식으로 묶은 것이다."**

### 1. Work summary
- 사용자가 지적한 대로 기존 Oh My 계열과 gstack 계열의 디버그 관련 스킬을 다시 확인했다.
- Oh My `debug`, `build-fix`, `doctor`, `git-master`, `team`, `pipeline`, `ralph`, `trace`, `tdd`, `ultraqa`에서 가져올 디버그 습관을 정리했다.
- gstack `investigate`, `freeze/guard/careful`, `browse/qa/qa-only`, `benchmark`, `learn`에서 가져올 검증/안전/학습 루틴을 정리했다.
- `mansu-debug-rootcause`에 `Source mapping`과 `Debug route selection`을 추가해서 어떤 버그에 어떤 기존 루틴을 참고해야 하는지 명시했다.

### 2. Verification
- `scripts/validate_mansu_debug_rootcause.sh`가 source mapping과 route selection 키워드를 검사하도록 보강했다.

### 3. Follow-up
- 실제 디버그 작업에서 `build-fix`, `doctor`, `benchmark`, `trace` 분기가 얼마나 자주 쓰이는지 관찰한 뒤, 필요하면 별도 Mansu debug 계열로 분리한다.

## 2026-04-24: Reframe `mansu-debug-rootcause` as a thin wrapper
> **"뼈대는 Oh My `debug`, 실행 게이트는 gstack `investigate`에서 가져온다."**

### 1. Work summary
- `mansu-debug-rootcause`가 자체 디버그 지식을 길게 품는 방식에서 벗어나도록 정리했다.
- 항상 먼저 읽어야 할 source skill을 Oh My `debug`와 `gstack-investigate`로 고정했다.
- Oh My `debug`의 6단계 루프를 outer skeleton으로 삼고, 각 단계마다 gstack gate를 덧씌우도록 바꿨다.
- `build-fix`, `doctor`, `git-master`, `browse/qa`, `benchmark`, `trace`, `team`은 조건부 route skill로만 읽게 했다.
- `agents/openai.yaml`도 "Oh My debug skeleton with gstack gates" 방향으로 맞췄다.

### 2. Verification
- `scripts/validate_mansu_debug_rootcause.sh`가 mandatory source skill loading과 wrapper 구조를 검사하도록 업데이트했다.

### 3. Follow-up
- 실제 사용하면서 매번 mandatory source skill을 읽는 비용이 너무 크면, source skill summary cache를 별도 reference로 둘지 검토한다.

## 2026-04-24: Harden `mansu-debug-rootcause` gates
> **"원본을 존중하되, 매번 통째로 읽지 않는다. 필수 게이트는 우리 스킬 안에 짧고 강하게 박는다."**

### 1. Work summary
- 멀티에이전트 리뷰에서 나온 6개 findings를 반영했다.
- 매번 Oh My `debug`와 `gstack-investigate` 전체를 읽는 방식에서, 필수 게이트는 내장하고 원본은 애매하거나 고위험일 때만 읽는 progressive disclosure 방식으로 바꿨다.
- `gstack-qa`가 root-cause 루프를 우회하지 않도록, 브라우저 증거는 `browse`/`qa-only`를 기본으로 하고 `qa`는 root cause 확인 후 또는 사용자가 명시한 경우로 제한했다.
- 회귀 테스트를 기본 필수로 바꾸고, 불가능할 때만 이유와 수동 검증 경로를 남기게 했다.
- `ISOLATE`와 `HYPOTHESIZE` 사이에 pattern-analysis checkpoint를 추가했다.
- hypothesis 이후 scope-lock 또는 skip reason을 필수로 바꿨다.
- `agents/openai.yaml`을 사용자-facing 문구로 바꾸고, 검증 스크립트가 frontmatter와 UI metadata까지 확인하게 했다.

### 2. Verification
- `scripts/validate_mansu_debug_rootcause.sh`가 frontmatter, section, route table, regression proof, scope-lock, report format, `openai.yaml` metadata를 검사하도록 강화됐다.

### 3. Follow-up
- 실제 디버그 작업에서 route table이 충분한지 관찰하고, 반복적으로 커지는 분기는 별도 `mansu-debug-*` 스킬로 분리한다.
