# 개발일지

## 2026-04-24: Harden `mansu-tdd-lite`
> **"lite는 가볍게 유지하되, 최소 계약은 모호하지 않게 만들었다."**

### 1. Work summary
- `mansu-tdd-strict`와 `mansu-tdd-lite`를 새 `mansu-skills` 저장소로 가져왔다.
- `mansu-tdd-lite`에 lightweight preflight, compact slice card, minimum slice close, closeout 규칙을 추가했다.
- 구조 계약을 검증하는 `scripts/validate_mansu_tdd_lite.sh`를 추가했다.
- 개선된 `mansu-tdd-lite`를 설치된 로컬 skill 경로에도 동기화했다.

### 2. Slice completed
- Slice 1: repo bootstrap + `mansu-tdd-lite` 최소 계약 보강 + 구조 검증 스크립트 추가

### 3. Verification
- RED: `scripts/validate_mansu_tdd_lite.sh` initial FAIL
- GREEN: `scripts/validate_mansu_tdd_lite.sh` PASS
- Sync check: repo version copied back to installed `mansu-tdd-lite`

### 4. Files
- `mansu-tdd-strict/**`
- `mansu-tdd-lite/SKILL.md`
- `mansu-tdd-lite/agents/openai.yaml`
- `scripts/validate_mansu_tdd_lite.sh`
- `PLAN.md`

### 5. Follow-up
- repository source와 installed skill source를 어떻게 동기화할지 정해야 한다.

## 2026-04-24: Reframe `mansu-tdd-lite`
> **"lite는 대충 하는 모드가 아니라, strict RED 테스트 강제만 완화하는 모드다."**

### 1. Work summary
- `mansu-tdd-lite`의 정체성을 다시 잡았다.
- 계획, vertical slice, review, QA, checkpoint, commit, worklog는 약화하지 않는다고 명시했다.
- 완화되는 부분은 매 slice마다 반드시 failing RED test로 시작해야 한다는 strict TDD 요구뿐이라고 정리했다.
- `agents/openai.yaml`과 lite 검증 스크립트도 새 원칙에 맞게 업데이트했다.

### 2. Verification
- `scripts/validate_mansu_tdd_lite.sh` PASS

### 3. Follow-up
- `mansu-tdd-strict`와 `mansu-tdd-lite`의 차이를 README나 skill map에 짧게 정리할 수 있다.

## 2026-04-24: Add `mansu-tdd-total`
> **"`total`은 super-executor가 아니라, 계획하고 slice별로 lite/strict를 고르는 router다."**

### 1. Work summary
- multiagent 검증으로 `total = planner/router`, `lite/strict = executor mode` 구조를 확인했다.
- `mansu-tdd-total`을 새 skill로 추가했다.
- `total`에 preflight, planning loop, dispatch rules, slice table, execution handoff, mode-change rules, closeout을 넣었다.
- `mansu-tdd-lite`와 `mansu-tdd-strict`에 Mansu TDD series 안에서의 역할을 추가했다.
- `total`과 series 구조를 검증하는 스크립트를 추가했다.
- repo 버전을 installed local skill 경로에도 동기화했다.

### 2. Slice completed
- Slice 1: `mansu-tdd-total` router 추가 + `lite/strict` executor 역할 보강 + validation script 추가

### 3. Verification
- `scripts/validate_mansu_tdd_total.sh` PASS
- `scripts/validate_mansu_tdd_lite.sh` PASS
- `scripts/validate_mansu_tdd_series.sh` PASS
- repo skill files and installed local skill files diff: clean
- `git diff --check` PASS

### 4. Files
- `mansu-tdd-total/SKILL.md`
- `mansu-tdd-total/agents/openai.yaml`
- `mansu-tdd-lite/SKILL.md`
- `mansu-tdd-lite/agents/openai.yaml`
- `mansu-tdd-strict/SKILL.md`
- `scripts/validate_mansu_tdd_total.sh`
- `scripts/validate_mansu_tdd_series.sh`
- `PLAN.md`

### 5. Follow-up
- `mansu-skills` repo를 원격 `main`에 올릴지, 현재 branch를 리뷰용으로 둘지 결정해야 한다.

## 2026-04-24: Lock Mansu TDD invariants
> **"`lite`와 `strict`의 차이는 품질 강도가 아니라 RED 테스트 강제 여부다."**

### 1. Work summary
- 외부 TDD/vertical slicing 자료와 현재 스킬 구조를 대조했다.
- `mansu-tdd-total`에 `Shared invariants` 섹션을 추가했다.
- `lite`는 lower-quality mode가 아니며 planning, slicing, review, QA, checkpoint, commit, work logging은 유지한다고 명시했다.
- `strict`는 meaningful failing RED test가 confidence/design/risk protection을 높이는 slice에 쓰는 모드라고 보강했다.
- validation script가 이 철학 문구를 확인하도록 업데이트했다.

### 2. Verification
- `scripts/validate_mansu_tdd_series.sh` PASS
- repo skill files and installed local skill files diff: clean
- `git diff --check` PASS

### 3. Follow-up
- 필요하면 이 철학을 README 또는 `mansu-tdd-total`의 prompt examples에도 짧게 반영할 수 있다.

## 2026-04-24: Add `mansu-debug-rootcause`
> **"버그는 기능 작업이 아니다. 원인을 확인하기 전에는 수정하지 않는다."**

### 1. Work summary
- 기존 Oh My Codex `debug` 스킬에서 `reproduce -> isolate -> hypothesize -> test -> fix -> verify` 흐름을 참고했다.
- `gstack-investigate`에서 `no fixes without root cause`, 3 hypotheses stop rule, minimal fix, fresh verification, debug report 원칙을 가져왔다.
- `mansu-debug-rootcause`를 새 skill로 추가했다.
- `mansu-tdd-*`와 달리 feature planning이 아니라 root-cause investigation 전용 축으로 분리했다.
- 전체 Mansu skill 검증 스크립트 `scripts/validate_mansu_skills.sh`를 추가했다.
- repo 버전을 installed local skill 경로에도 동기화했다.

### 2. Slice completed
- Slice 1: `mansu-debug-rootcause` 추가 + debug validation script 추가 + installed local skill sync

### 3. Verification
- RED: `scripts/validate_mansu_debug_rootcause.sh` initial FAIL
- GREEN: `scripts/validate_mansu_debug_rootcause.sh` PASS
- `scripts/validate_mansu_skills.sh` PASS
- repo debug skill and installed local debug skill diff: clean

### 4. Files
- `mansu-debug-rootcause/SKILL.md`
- `mansu-debug-rootcause/agents/openai.yaml`
- `scripts/validate_mansu_debug_rootcause.sh`
- `scripts/validate_mansu_skills.sh`
- `PLAN.md`

### 5. Follow-up
- 실제 버그 작업에서 써본 뒤, report format과 escalation 기준을 더 날카롭게 다듬을 수 있다.

## 2026-04-24: Map debug skill sources
> **"`mansu-debug-rootcause`는 독자 발명이 아니라, Oh My와 gstack의 디버그 루틴을 Mansu식으로 묶은 것이다."**

### 1. Work summary
- 사용자가 지적한 대로 기존 Oh My 계열과 gstack 계열의 디버그 관련 스킬을 다시 확인했다.
- Oh My `debug`, `build-fix`, `doctor`, `git-master`, `team`, `pipeline`, `ralph`, `trace`, `tdd`, `ultraqa`에서 가져올 디버그 습관을 정리했다.
- gstack `investigate`, `freeze/guard/careful`, `browse/qa/qa-only`, `benchmark`, `learn`에서 가져올 검증/안전/학습 루틴을 정리했다.
- `mansu-debug-rootcause`에 `Source mapping`과 `Debug route selection`을 추가해서 어떤 버그에 어떤 기존 루틴을 참고해야 하는지 명시했다.

### 2. Verification
- `scripts/validate_mansu_debug_rootcause.sh`가 source mapping과 route selection 키워드를 검사하도록 보강했다.

### 3. Follow-up
- 실제 디버그 작업에서 `build-fix`, `doctor`, `benchmark`, `trace` 분기가 얼마나 자주 쓰이는지 관찰한 뒤, 필요하면 별도 Mansu debug 계열로 분리한다.

## 2026-04-24: Reframe `mansu-debug-rootcause` as a thin wrapper
> **"뼈대는 Oh My `debug`, 실행 게이트는 gstack `investigate`에서 가져온다."**

### 1. Work summary
- `mansu-debug-rootcause`가 자체 디버그 지식을 길게 품는 방식에서 벗어나도록 정리했다.
- 항상 먼저 읽어야 할 source skill을 Oh My `debug`와 `gstack-investigate`로 고정했다.
- Oh My `debug`의 6단계 루프를 outer skeleton으로 삼고, 각 단계마다 gstack gate를 덧씌우도록 바꿨다.
- `build-fix`, `doctor`, `git-master`, `browse/qa`, `benchmark`, `trace`, `team`은 조건부 route skill로만 읽게 했다.
- `agents/openai.yaml`도 "Oh My debug skeleton with gstack gates" 방향으로 맞췄다.

### 2. Verification
- `scripts/validate_mansu_debug_rootcause.sh`가 mandatory source skill loading과 wrapper 구조를 검사하도록 업데이트했다.

### 3. Follow-up
- 실제 사용하면서 매번 mandatory source skill을 읽는 비용이 너무 크면, source skill summary cache를 별도 reference로 둘지 검토한다.

## 2026-04-24: Harden `mansu-debug-rootcause` gates
> **"원본을 존중하되, 매번 통째로 읽지 않는다. 필수 게이트는 우리 스킬 안에 짧고 강하게 박는다."**

### 1. Work summary
- 멀티에이전트 리뷰에서 나온 6개 findings를 반영했다.
- 매번 Oh My `debug`와 `gstack-investigate` 전체를 읽는 방식에서, 필수 게이트는 내장하고 원본은 애매하거나 고위험일 때만 읽는 progressive disclosure 방식으로 바꿨다.
- `gstack-qa`가 root-cause 루프를 우회하지 않도록, 브라우저 증거는 `browse`/`qa-only`를 기본으로 하고 `qa`는 root cause 확인 후 또는 사용자가 명시한 경우로 제한했다.
- 회귀 테스트를 기본 필수로 바꾸고, 불가능할 때만 이유와 수동 검증 경로를 남기게 했다.
- `ISOLATE`와 `HYPOTHESIZE` 사이에 pattern-analysis checkpoint를 추가했다.
- hypothesis 이후 scope-lock 또는 skip reason을 필수로 바꿨다.
- `agents/openai.yaml`을 사용자-facing 문구로 바꾸고, 검증 스크립트가 frontmatter와 UI metadata까지 확인하게 했다.

### 2. Verification
- `scripts/validate_mansu_debug_rootcause.sh`가 frontmatter, section, route table, regression proof, scope-lock, report format, `openai.yaml` metadata를 검사하도록 강화됐다.

### 3. Follow-up
- 실제 디버그 작업에서 route table이 충분한지 관찰하고, 반복적으로 커지는 분기는 별도 `mansu-debug-*` 스킬로 분리한다.

## 2026-04-24: Add `mansu-web-verify`
> **"사용자처럼 눌러보되, 무한정 돌지 않는다. 안전한 범위 안에서 끝까지 검증한다."**

### 1. Work summary
- 실제 사용자처럼 웹앱을 검증하는 `mansu-web-verify` 스킬을 추가했다.
- 스킬 성격을 `report-only` 기본값으로 고정하고, `fix-approved`와 `post-deploy` 모드를 분리했다.
- 모든 페이즈에 무한 Ralph를 적용하지 않고, planned safe checks 안에서만 bounded Ralph loop를 쓰도록 설계했다.
- 프로젝트 구조를 먼저 읽고 surface map과 verification plan을 만든 뒤 브라우저 검증으로 들어가게 했다.
- 브라우저 기능, 안정성, 성능, 반응형, 기본 보안 검증을 한 리포트로 묶었다.
- 위험 액션, 결제, 이메일, SMS, production write, destructive action은 명시 승인 없이는 중단하게 했다.
- 불명확한 버그는 직접 고치지 않고 `mansu-debug-rootcause`로 라우팅하게 했다.

### 2. Verification
- `scripts/validate_mansu_web_verify.sh`를 추가했다.
- 전체 `scripts/validate_mansu_skills.sh`가 `mansu-web-verify` 구조도 함께 검증하게 했다.

### 3. Follow-up
- 실제 웹앱에서 사용해본 뒤 report format과 performance/security 기준을 더 날카롭게 다듬는다.

## 2026-04-24: Reframe `mansu-web-verify` as an orchestrator
> **"우리가 모든 검증 지식을 품지 않는다. 우리는 계획하고, 안전선을 치고, 맞는 전문 스킬로 넘긴다."**

### 1. Work summary
- Oh My와 gstack의 관련 웹 검증 스킬을 다시 대조했다.
- `mansu-web-verify`를 거대한 QA 본문이 아니라 thin orchestrator로 재정의했다.
- Mansu가 직접 책임지는 영역을 preflight, surface map, verification plan, safety boundary, evidence, issue routing, final report로 제한했다.
- 브라우저 실행은 gstack `browse`, report-only QA는 gstack `qa-only`, 수정 루프는 승인된 경우의 gstack `qa`, 성능은 gstack `benchmark`, 깊은 보안은 gstack `cso`, 배포 후 감시는 gstack `canary`, 반복 가능한 E2E는 Oh My `playwright`로 라우팅하게 했다.
- `diff-aware`, `quick`, `full`, `regression` 모드를 추가해서 검증 범위를 더 명확히 나누었다.
- 증거 규칙과 성능 threshold를 추가해서 리포트가 감상문이 아니라 재현 가능한 검증 자료가 되도록 했다.

### 2. Verification
- `scripts/validate_mansu_web_verify.sh`가 source skill map, Mansu-owned orchestration, 추가 modes, source skills, performance thresholds, report fields, UI metadata를 검사하도록 강화됐다.

### 3. Follow-up
- 실제 프로젝트에서 써본 뒤 source skill delegation이 너무 잦거나 부족한 지점을 관찰하고, 반복 분기는 별도 `mansu-web-*` 스킬로 분리한다.

## 2026-04-24: Document Mansu philosophy
> **"Mansu는 모든 것을 직접 발명하지 않는다. 좋은 스킬을 해부하고, 필요한 순간에 정확히 조합한다."**

### 1. Work summary
- `MANSU_PHILOSOPHY.md`를 추가해서 Mansu 시리즈의 공통 철학을 문서화했다.
- Mansu의 책임을 planning, safety boundary, role separation, routing, verification, checkpoint, worklog, final accountability로 정리했다.
- 좋은 기존 Oh My/gstack/source skill이 있으면 전체를 복사하지 말고, 필수 게이트만 내장하고 깊은 실행은 source skill로 위임한다는 원칙을 명시했다.
- `mansu-tdd-total`, `mansu-debug-rootcause`, `mansu-web-verify`에도 이 철학을 짧게 반영했다.

### 2. Verification
- 전체 Mansu skill validation을 다시 실행한다.

### 3. Follow-up
- 이후 새 `mansu-*` 스킬을 만들 때는 먼저 source skill map을 작성하고, Mansu-owned boundary를 따로 적는다.

## 2026-04-24: Align `mansu-debug-rootcause` with Mansu philosophy
> **"디버그 스킬도 만능 디버거가 아니라 root-cause 규율을 지키는 오케스트레이터다."**

### 1. Work summary
- `mansu-debug-rootcause`의 `Source relationship`을 `Source skill map`으로 바꾸었다.
- Oh My `debug`, gstack `investigate`, `build-fix`, `doctor`, `git-master`, `browse/qa-only`, `qa`, `benchmark`, `trace`, `team`을 언제 읽거나 호출할지 표로 정리했다.
- `Mansu-owned boundary`를 추가해서 Mansu가 직접 책임질 영역과 source skill에 위임할 영역을 분리했다.
- 핵심 원칙을 `thin where it can be thin and strict where it must be strict`로 고정했다.

### 2. Verification
- `scripts/validate_mansu_debug_rootcause.sh`가 source skill map과 Mansu-owned boundary를 검사하도록 업데이트했다.

### 3. Follow-up
- 실제 디버그 작업에서 source skill map이 너무 넓거나 좁은지 관찰하고, 자주 반복되는 분기는 별도 `mansu-debug-*` 스킬로 분리한다.

## 2026-04-24: Add root README
> **"Mansu는 좋은 source skill을 더 안전하게 조합하기 위한 운영 레이어다."**

### 1. Work summary
- `mansu-skills` 루트 README를 추가했다.
- 상단에서 Mansu의 정체성, 설치, agent용 시작 프롬프트를 바로 볼 수 있게 했다.
- `mansu-*` 스킬 목록, 추천 라우팅, source skill policy, 검증 명령어, repo map을 정리했다.
- README가 `MANSU_PHILOSOPHY.md`와 같은 철학을 공유하도록 맞췄다.

### 2. Verification
- 전체 Mansu skill validation과 markdown diff check를 실행한다.

### 3. Follow-up
- 원격 `main`을 canonical로 쓸지, 현재 `codex/*` branch를 리뷰 branch로 유지할지 결정한다.

## 2026-04-24: Strengthen `mansu-tdd-total` planning gate
> **"계획은 예시가 아니라 게이트다. 구현은 accepted plan과 slice table 뒤에만 온다."**

### 1. Work summary
- `mansu-tdd-total`의 planning loop를 강화했다.
- non-trivial work에서 단일 TODO식 계획을 금지하고, Planner / Critics / Synthesizer 역할을 필수로 매핑하게 했다.
- 정확한 Prometheus, Metis, Momus 또는 gstack 도구명이 없더라도 같은 책임을 맡는 도구/관점을 `PLAN.md`에 기록하게 했다.
- accepted plan 요구사항에 goal, non-goals, DDD-style boundary, architecture/module boundary, source skills, risks, validation strategy, docs/worklog target, approval status를 추가했다.
- README에도 multi-role critique loop를 Mansu 핵심 철학으로 노출했다.

### 2. Verification
- `scripts/validate_mansu_tdd_total.sh`가 required planning roles와 accepted plan requirements를 검사하도록 강화됐다.

### 3. Follow-up
- 실제 기능 작업에서 `PLAN.md` 템플릿이 필요해지면 `mansu-tdd-total`에 accepted plan template을 추가할지 검토한다.

## 2026-04-24: Promote `mansu-skills` to main
> **"완성된 Mansu 스킬은 작업 브랜치가 아니라 main을 기준으로 공유한다."**

### 1. Work summary
- 완성된 `codex/mansu-tdd-lite-hardening` 상태에서 `main` 브랜치를 만들었다.
- README에 canonical branch가 `main`임을 명시했다.
- `codex/*` 브랜치는 임시 작업 브랜치이고, 완료된 스킬 변경은 `main`에 반영한다는 branch policy를 추가했다.

### 2. Verification
- 전체 Mansu skill validation과 `git diff --check`를 실행한다.

### 3. Follow-up
- 필요하면 GitHub repository default branch도 `main`으로 맞춘다.

## 2026-04-24: Strengthen sequential slice gate
> **"다음 slice는 이전 slice가 닫힌 뒤에만 시작한다."**

### 1. Work summary
- `mansu-tdd-total`에 `Sequential slice gate`를 추가했다.
- slice N+1은 slice N이 validation, review, QA, checkpoint, `PLAN.md`, worklog, commit/no-commit reason까지 닫힌 뒤에만 시작하게 했다.
- `mansu-tdd-lite`에도 current lite slice close 전 다음 slice 금지 문구를 명시했다.
- README에도 sequential slice 기본 원칙을 노출했다.

### 2. Verification
- `scripts/validate_mansu_tdd_total.sh`와 `scripts/validate_mansu_tdd_lite.sh`가 sequential slice gate 문구를 검사하도록 강화됐다.

### 3. Follow-up
- 실제 작업에서 병렬 slice가 필요하면 dependency-free order를 사용자가 명시 승인했을 때만 허용한다.

## 2026-04-25: Reframe planning gate as automatic execution readiness
> **"계획은 사람 승인 대기실이 아니라, 자동 실행해도 안전한 상태를 만드는 품질 게이트다."**

### 1. Work summary
- `mansu-tdd-total`의 `accepted plan` 표현을 `execution-ready plan`으로 바꾸었다.
- non-trivial work에서 planning role, critique, slice table이 끝나면 자동 실행하는 것이 기본임을 명시했다.
- 사람 승인으로 멈추는 경우를 `명시적 승인 게이트`, `위험 작업`, `미해결 blocker`로 제한했다.
- README와 `MANSU_PHILOSOPHY.md`에도 같은 철학을 반영했다.
- `mansu-tdd-lite`와 `mansu-tdd-strict`의 handoff 표현도 execution-ready plan 기준으로 맞췄다.

### 2. Verification
- `scripts/validate_mansu_tdd_total.sh`가 execution-ready plan과 자동 실행 게이트를 검사하도록 업데이트했다.

### 3. Follow-up
- 실제 작업 보고서에는 `실제 multi-agent 사용 여부`와 `역할 시뮬레이션 여부`를 명시하게 할지 검토한다.

## 2026-04-27: Add real critic agent wait gate
> **"자동 실행은 critic을 무시한다는 뜻이 아니다."**

### 1. Work summary
- `mansu-tdd-total`에 `Real critic agent gate`를 추가했다.
- 실제 critic sub-agent나 외부 critic tool을 시작한 경우, 그 결과를 planning gate 입력으로 취급하게 했다.
- critic이 pending이면 execution-ready plan을 잠그거나 구현을 시작하지 못하게 했다.
- critic 실패/timeout fallback은 `PLAN.md`에 기록하고, 고위험 미해결 질문은 `blocked`로 남기게 했다.
- README와 `MANSU_PHILOSOPHY.md`에도 같은 원칙을 반영했다.

### 2. Verification
- `scripts/validate_mansu_tdd_total.sh`가 real critic agent gate 문구를 검사하도록 업데이트했다.

### 3. Follow-up
- 실제 작업 보고서에 critic 이름, 상태, fallback 여부를 항상 포함시키는 템플릿을 추가할지 검토한다.

## 2026-04-27: Add final validation and report closeout
> **"작업은 구현이 아니라 검증, 커밋, 남은 리스크 보고까지 끝나야 닫힌다."**

### 1. Work summary
- `mansu-tdd-total` closeout에 project-level build/test/type/lint suite 실행을 추가했다.
- 사용자-visible 변경이 있으면 final QA 또는 browser verification을 하게 했다.
- 최종 검증 뒤 commit하거나, commit을 미룬 이유를 기록하게 했다.
- 최종 보고에 검증한 것, 검증하지 못한 것, 남은 리스크, 후속 확인 사항을 반드시 포함하게 했다.
- `mansu-tdd-lite`와 strict gate reference에도 같은 closeout 원칙을 반영했다.

### 2. Verification
- `scripts/validate_mansu_tdd_total.sh`와 `scripts/validate_mansu_tdd_lite.sh`가 final validation/report closeout 문구를 검사하도록 업데이트했다.

### 3. Follow-up
- 실제 프로젝트별로 `build/test/type/lint` 명령이 다를 수 있으니, 각 repo의 PLAN.md나 README에 권장 최종 검증 명령을 남기는 방식을 검토한다.
