# 개발일지

## 2026-05-02: Add code construction router
> **"Mansu는 코딩 순서를 하드코딩하지 않는다. 현재 개발 단계가 어디인지 보고, 맞는 source skill을 읽고, Mansu invariants를 적용한다."**

### 1. Work summary
- `addyosmani/agent-skills`에서 `planning-and-task-breakdown`, `incremental-implementation`, `context-engineering`, `source-driven-development`의 coding-order 관점을 참고했다.
- `mansu-operating-model/references/CODE_CONSTRUCTION_ORDER.md`를 dynamic router로 추가했다.
- 고정된 코드 작성 순서 대신, 현재 phase를 감지하고 관련 source skill을 읽은 뒤 필요한 phase-specific action만 추출하도록 정리했다.
- `SOURCE_SKILL_CATALOG.md`에 `addyosmani/agent-skills`를 세 번째 source family로 추가했다.
- `mansu-tdd-total`, `mansu-tdd-lite`, `mansu-tdd-strict`가 construction router를 통해 현재 단계와 source skill을 기록하도록 업데이트했다.
- `mansu-start`가 addyosmani/agent-skills의 존재, freshness, phase-name drift도 확인하도록 업데이트했다.
- 실제 GitHub skills 목록 기준으로 `idea-refine`, `code-simplification`, `ci-cd-and-automation`, `deprecation-and-migration`까지 빠지지 않게 catalog/router를 보강했다.
- README, `MANSU_PHILOSOPHY.md`, validators가 이 기준을 보존하게 했다.

### 2. Verification
- `scripts/validate_mansu_operating_model.sh` PASS
- `scripts/validate_mansu_doc_system.sh` PASS
- `scripts/validate_mansu_tdd_series.sh` PASS
- `scripts/validate_mansu_skills.sh` PASS
- `git diff --check` PASS

### 3. Follow-up
- 실제 feature 작업에서 construction-order notes가 `PLAN.md` slice table에 충분히 자연스럽게 들어가는지 관찰한다.
- 필요하면 별도 `mansu-code-construction` skill로 분리하되, 지금은 reference로 유지한다.

## 2026-05-02: Add source skill catalog
> **"Mansu는 gstack과 Oh My를 복제하지 않는다. 무엇이 있는지 알고, 필요할 때 정확히 조합한다."**

### 1. Work summary
- `mansu-operating-model/references/SOURCE_SKILL_CATALOG.md`를 추가했다.
- gstack의 CEO/디자인/엔지니어링/QA/보안/성능/배포/학습 계열 스킬을 capability map으로 정리했다.
- Oh My / OMO / OMC 계열의 autopilot, ultrawork, ralph, tdd, debug, research, ultraqa 등 실행 모드와 습관형 스킬을 routing map으로 정리했다.
- Mansu composition recipes를 추가해서 feature/refactor, UI/UX, bug/regression, web verification, ship/learn 상황에서 어떤 source skill을 조합할지 남겼다.
- gstack과 Oh My / OMO / OMC는 계속 바뀌는 source family이므로, `mansu-start`가 둘 다 freshness check에 포함하도록 업데이트했다.
- README, `MANSU_PHILOSOPHY.md`, `mansu-operating-model`이 source skill catalog를 명시하도록 업데이트했다.
- 검증 스크립트가 catalog 존재와 핵심 source route를 확인하도록 강화했다.

### 2. Verification
- `scripts/validate_mansu_operating_model.sh` PASS
- `scripts/validate_mansu_doc_system.sh` PASS
- `scripts/validate_mansu_skills.sh` PASS
- `git diff --check` PASS

### 3. Follow-up
- 실제 작업에서 catalog route가 너무 넓거나 애매한 부분을 관찰하고, 반복적으로 쓰는 조합은 `mansu-*` thin wrapper로 분리한다.
- `mansu-start`가 gstack/Oh My 계열 source skill availability를 더 잘 점검하도록 확장할 수 있다.

## 2026-05-02: Add `mansu-operating-model`
> **"Mansu는 이제 단순 스킬 묶음이 아니라, project `AGENTS.md`와 `CODING_RULES.md`를 규율하는 canonical doctrine을 가진다."**

### 1. Work summary
- `mansu-operating-model`을 새 skill로 추가했다.
- 이 skill 안에 canonical doctrine 본문과 project `AGENTS.md` / `CODING_RULES.md` reference templates를 넣었다.
- doctrine, router, local-manual 구조가 README와 `MANSU_PHILOSOPHY.md`에도 드러나도록 정리했다.
- `validate_mansu_operating_model.sh`와 `validate_mansu_doc_system.sh`를 추가했다.
- top-level `scripts/validate_mansu_skills.sh`가 새 doctrine/document validators를 함께 돌리도록 연결했다.

### 2. Verification
- `scripts/validate_mansu_operating_model.sh` PASS
- `scripts/validate_mansu_doc_system.sh` PASS
- `scripts/validate_mansu_skills.sh` PASS
- `git diff --check` PASS

### 3. Follow-up
- 실제 프로젝트 fixture 예시까지 검증하는 문서-system validator를 추가할지 검토한다.
- `mansu-start`가 새 프로젝트 bootstrap 때 `mansu-operating-model`을 함께 추천하도록 확장할지 검토한다.

## 2026-04-24: Harden `mansu-tdd-lite`
> **"lite는 가볍게 유지하되, 최소 계약은 모호하지 않게 만들었다."**

### 1. Work summary
- `mansu-tdd-strict`와 `mansu-tdd-lite`를 새 `mansu-skills` 저장소로 가져왔다.
- `mansu-tdd-lite`에 lightweight preflight, compact slice card, minimum slice close, closeout 규칙을 추가했다.
- 구조 계약을 검증하는 `scripts/validate_mansu_tdd_lite.sh`를 추가했다.
- 개선된 `mansu-tdd-lite`를 설치된 로컬 skill 경로에도 동기화했다.

### 2. Slice completed
- Slice 1: repo bootstrap + `mansu-tdd-lite` 최소 계약 보강 + 구조 검증 스크립트 추가

### 3. Verification
- RED: `scripts/validate_mansu_tdd_lite.sh` initial FAIL
- GREEN: `scripts/validate_mansu_tdd_lite.sh` PASS
- Sync check: repo version copied back to installed `mansu-tdd-lite`

### 4. Files
- `mansu-tdd-strict/**`
- `mansu-tdd-lite/SKILL.md`
- `mansu-tdd-lite/agents/openai.yaml`
- `scripts/validate_mansu_tdd_lite.sh`
- `PLAN.md`

### 5. Follow-up
- repository source와 installed skill source를 어떻게 동기화할지 정해야 한다.

## 2026-04-24: Reframe `mansu-tdd-lite`
> **"lite는 대충 하는 모드가 아니라, strict RED 테스트 강제만 완화하는 모드다."**

### 1. Work summary
- `mansu-tdd-lite`의 정체성을 다시 잡았다.
- 계획, vertical slice, review, QA, checkpoint, commit, worklog는 약화하지 않는다고 명시했다.
- 완화되는 부분은 매 slice마다 반드시 failing RED test로 시작해야 한다는 strict TDD 요구뿐이라고 정리했다.
- `agents/openai.yaml`과 lite 검증 스크립트도 새 원칙에 맞게 업데이트했다.

### 2. Verification
- `scripts/validate_mansu_tdd_lite.sh` PASS

### 3. Follow-up
- `mansu-tdd-strict`와 `mansu-tdd-lite`의 차이를 README나 skill map에 짧게 정리할 수 있다.

## 2026-04-24: Add `mansu-tdd-total`
> **"`total`은 super-executor가 아니라, 계획하고 slice별로 lite/strict를 고르는 router다."**

### 1. Work summary
- multiagent 검증으로 `total = planner/router`, `lite/strict = executor mode` 구조를 확인했다.
- `mansu-tdd-total`을 새 skill로 추가했다.
- `total`에 preflight, planning loop, dispatch rules, slice table, execution handoff, mode-change rules, closeout을 넣었다.
- `mansu-tdd-lite`와 `mansu-tdd-strict`에 Mansu TDD series 안에서의 역할을 추가했다.
- `total`과 series 구조를 검증하는 스크립트를 추가했다.
- repo 버전을 installed local skill 경로에도 동기화했다.

### 2. Slice completed
- Slice 1: `mansu-tdd-total` router 추가 + `lite/strict` executor 역할 보강 + validation script 추가

### 3. Verification
- `scripts/validate_mansu_tdd_total.sh` PASS
- `scripts/validate_mansu_tdd_lite.sh` PASS
- `scripts/validate_mansu_tdd_series.sh` PASS
- repo skill files and installed local skill files diff: clean
- `git diff --check` PASS

### 4. Files
- `mansu-tdd-total/SKILL.md`
- `mansu-tdd-total/agents/openai.yaml`
- `mansu-tdd-lite/SKILL.md`
- `mansu-tdd-lite/agents/openai.yaml`
- `mansu-tdd-strict/SKILL.md`
- `scripts/validate_mansu_tdd_total.sh`
- `scripts/validate_mansu_tdd_series.sh`
- `PLAN.md`

### 5. Follow-up
- `mansu-skills` repo를 원격 `main`에 올릴지, 현재 branch를 리뷰용으로 둘지 결정해야 한다.

## 2026-04-24: Lock Mansu TDD invariants
> **"`lite`와 `strict`의 차이는 품질 강도가 아니라 RED 테스트 강제 여부다."**

### 1. Work summary
- 외부 TDD/vertical slicing 자료와 현재 스킬 구조를 대조했다.
- `mansu-tdd-total`에 `Shared invariants` 섹션을 추가했다.
- `lite`는 lower-quality mode가 아니며 planning, slicing, review, QA, checkpoint, commit, work logging은 유지한다고 명시했다.
- `strict`는 meaningful failing RED test가 confidence/design/risk protection을 높이는 slice에 쓰는 모드라고 보강했다.
- validation script가 이 철학 문구를 확인하도록 업데이트했다.

### 2. Verification
- `scripts/validate_mansu_tdd_series.sh` PASS
- repo skill files and installed local skill files diff: clean
- `git diff --check` PASS

### 3. Follow-up
- 필요하면 이 철학을 README 또는 `mansu-tdd-total`의 prompt examples에도 짧게 반영할 수 있다.

## 2026-04-24: Add `mansu-debug-rootcause`
> **"버그는 기능 작업이 아니다. 원인을 확인하기 전에는 수정하지 않는다."**

### 1. Work summary
- 기존 Oh My Codex `debug` 스킬에서 `reproduce -> isolate -> hypothesize -> test -> fix -> verify` 흐름을 참고했다.
- `gstack-investigate`에서 `no fixes without root cause`, 3 hypotheses stop rule, minimal fix, fresh verification, debug report 원칙을 가져왔다.
- `mansu-debug-rootcause`를 새 skill로 추가했다.
- `mansu-tdd-*`와 달리 feature planning이 아니라 root-cause investigation 전용 축으로 분리했다.
- 전체 Mansu skill 검증 스크립트 `scripts/validate_mansu_skills.sh`를 추가했다.
- repo 버전을 installed local skill 경로에도 동기화했다.

### 2. Slice completed
- Slice 1: `mansu-debug-rootcause` 추가 + debug validation script 추가 + installed local skill sync

### 3. Verification
- RED: `scripts/validate_mansu_debug_rootcause.sh` initial FAIL
- GREEN: `scripts/validate_mansu_debug_rootcause.sh` PASS
- `scripts/validate_mansu_skills.sh` PASS
- repo debug skill and installed local debug skill diff: clean

### 4. Files
- `mansu-debug-rootcause/SKILL.md`
- `mansu-debug-rootcause/agents/openai.yaml`
- `scripts/validate_mansu_debug_rootcause.sh`
- `scripts/validate_mansu_skills.sh`
- `PLAN.md`

### 5. Follow-up
- 실제 버그 작업에서 써본 뒤, report format과 escalation 기준을 더 날카롭게 다듬을 수 있다.

## 2026-04-24: Map debug skill sources
> **"`mansu-debug-rootcause`는 독자 발명이 아니라, Oh My와 gstack의 디버그 루틴을 Mansu식으로 묶은 것이다."**

### 1. Work summary
- 사용자가 지적한 대로 기존 Oh My 계열과 gstack 계열의 디버그 관련 스킬을 다시 확인했다.
- Oh My `debug`, `build-fix`, `doctor`, `git-master`, `team`, `pipeline`, `ralph`, `trace`, `tdd`, `ultraqa`에서 가져올 디버그 습관을 정리했다.
- gstack `investigate`, `freeze/guard/careful`, `browse/qa/qa-only`, `benchmark`, `learn`에서 가져올 검증/안전/학습 루틴을 정리했다.
- `mansu-debug-rootcause`에 `Source mapping`과 `Debug route selection`을 추가해서 어떤 버그에 어떤 기존 루틴을 참고해야 하는지 명시했다.

### 2. Verification
- `scripts/validate_mansu_debug_rootcause.sh`가 source mapping과 route selection 키워드를 검사하도록 보강했다.

### 3. Follow-up
- 실제 디버그 작업에서 `build-fix`, `doctor`, `benchmark`, `trace` 분기가 얼마나 자주 쓰이는지 관찰한 뒤, 필요하면 별도 Mansu debug 계열로 분리한다.

## 2026-04-24: Reframe `mansu-debug-rootcause` as a thin wrapper
> **"뼈대는 Oh My `debug`, 실행 게이트는 gstack `investigate`에서 가져온다."**

### 1. Work summary
- `mansu-debug-rootcause`가 자체 디버그 지식을 길게 품는 방식에서 벗어나도록 정리했다.
- 항상 먼저 읽어야 할 source skill을 Oh My `debug`와 `gstack-investigate`로 고정했다.
- Oh My `debug`의 6단계 루프를 outer skeleton으로 삼고, 각 단계마다 gstack gate를 덧씌우도록 바꿨다.
- `build-fix`, `doctor`, `git-master`, `browse/qa`, `benchmark`, `trace`, `team`은 조건부 route skill로만 읽게 했다.
- `agents/openai.yaml`도 "Oh My debug skeleton with gstack gates" 방향으로 맞췄다.

### 2. Verification
- `scripts/validate_mansu_debug_rootcause.sh`가 mandatory source skill loading과 wrapper 구조를 검사하도록 업데이트했다.

### 3. Follow-up
- 실제 사용하면서 매번 mandatory source skill을 읽는 비용이 너무 크면, source skill summary cache를 별도 reference로 둘지 검토한다.

## 2026-04-24: Harden `mansu-debug-rootcause` gates
> **"원본을 존중하되, 매번 통째로 읽지 않는다. 필수 게이트는 우리 스킬 안에 짧고 강하게 박는다."**

### 1. Work summary
- 멀티에이전트 리뷰에서 나온 6개 findings를 반영했다.
- 매번 Oh My `debug`와 `gstack-investigate` 전체를 읽는 방식에서, 필수 게이트는 내장하고 원본은 애매하거나 고위험일 때만 읽는 progressive disclosure 방식으로 바꿨다.
- `gstack-qa`가 root-cause 루프를 우회하지 않도록, 브라우저 증거는 `browse`/`qa-only`를 기본으로 하고 `qa`는 root cause 확인 후 또는 사용자가 명시한 경우로 제한했다.
- 회귀 테스트를 기본 필수로 바꾸고, 불가능할 때만 이유와 수동 검증 경로를 남기게 했다.
- `ISOLATE`와 `HYPOTHESIZE` 사이에 pattern-analysis checkpoint를 추가했다.
- hypothesis 이후 scope-lock 또는 skip reason을 필수로 바꿨다.
- `agents/openai.yaml`을 사용자-facing 문구로 바꾸고, 검증 스크립트가 frontmatter와 UI metadata까지 확인하게 했다.

### 2. Verification
- `scripts/validate_mansu_debug_rootcause.sh`가 frontmatter, section, route table, regression proof, scope-lock, report format, `openai.yaml` metadata를 검사하도록 강화됐다.

### 3. Follow-up
- 실제 디버그 작업에서 route table이 충분한지 관찰하고, 반복적으로 커지는 분기는 별도 `mansu-debug-*` 스킬로 분리한다.

## 2026-04-24: Add `mansu-web-verify`
> **"사용자처럼 눌러보되, 무한정 돌지 않는다. 안전한 범위 안에서 끝까지 검증한다."**

### 1. Work summary
- 실제 사용자처럼 웹앱을 검증하는 `mansu-web-verify` 스킬을 추가했다.
- 스킬 성격을 `report-only` 기본값으로 고정하고, `fix-approved`와 `post-deploy` 모드를 분리했다.
- 모든 페이즈에 무한 Ralph를 적용하지 않고, planned safe checks 안에서만 bounded Ralph loop를 쓰도록 설계했다.
- 프로젝트 구조를 먼저 읽고 surface map과 verification plan을 만든 뒤 브라우저 검증으로 들어가게 했다.
- 브라우저 기능, 안정성, 성능, 반응형, 기본 보안 검증을 한 리포트로 묶었다.
- 위험 액션, 결제, 이메일, SMS, production write, destructive action은 명시 승인 없이는 중단하게 했다.
- 불명확한 버그는 직접 고치지 않고 `mansu-debug-rootcause`로 라우팅하게 했다.

### 2. Verification
- `scripts/validate_mansu_web_verify.sh`를 추가했다.
- 전체 `scripts/validate_mansu_skills.sh`가 `mansu-web-verify` 구조도 함께 검증하게 했다.

### 3. Follow-up
- 실제 웹앱에서 사용해본 뒤 report format과 performance/security 기준을 더 날카롭게 다듬는다.

## 2026-04-24: Reframe `mansu-web-verify` as an orchestrator
> **"우리가 모든 검증 지식을 품지 않는다. 우리는 계획하고, 안전선을 치고, 맞는 전문 스킬로 넘긴다."**

### 1. Work summary
- Oh My와 gstack의 관련 웹 검증 스킬을 다시 대조했다.
- `mansu-web-verify`를 거대한 QA 본문이 아니라 thin orchestrator로 재정의했다.
- Mansu가 직접 책임지는 영역을 preflight, surface map, verification plan, safety boundary, evidence, issue routing, final report로 제한했다.
- 브라우저 실행은 gstack `browse`, report-only QA는 gstack `qa-only`, 수정 루프는 승인된 경우의 gstack `qa`, 성능은 gstack `benchmark`, 깊은 보안은 gstack `cso`, 배포 후 감시는 gstack `canary`, 반복 가능한 E2E는 Oh My `playwright`로 라우팅하게 했다.
- `diff-aware`, `quick`, `full`, `regression` 모드를 추가해서 검증 범위를 더 명확히 나누었다.
- 증거 규칙과 성능 threshold를 추가해서 리포트가 감상문이 아니라 재현 가능한 검증 자료가 되도록 했다.

### 2. Verification
- `scripts/validate_mansu_web_verify.sh`가 source skill map, Mansu-owned orchestration, 추가 modes, source skills, performance thresholds, report fields, UI metadata를 검사하도록 강화됐다.

### 3. Follow-up
- 실제 프로젝트에서 써본 뒤 source skill delegation이 너무 잦거나 부족한 지점을 관찰하고, 반복 분기는 별도 `mansu-web-*` 스킬로 분리한다.

## 2026-04-24: Document Mansu philosophy
> **"Mansu는 모든 것을 직접 발명하지 않는다. 좋은 스킬을 해부하고, 필요한 순간에 정확히 조합한다."**

### 1. Work summary
- `MANSU_PHILOSOPHY.md`를 추가해서 Mansu 시리즈의 공통 철학을 문서화했다.
- Mansu의 책임을 planning, safety boundary, role separation, routing, verification, checkpoint, worklog, final accountability로 정리했다.
- 좋은 기존 Oh My/gstack/source skill이 있으면 전체를 복사하지 말고, 필수 게이트만 내장하고 깊은 실행은 source skill로 위임한다는 원칙을 명시했다.
- `mansu-tdd-total`, `mansu-debug-rootcause`, `mansu-web-verify`에도 이 철학을 짧게 반영했다.

### 2. Verification
- 전체 Mansu skill validation을 다시 실행한다.

### 3. Follow-up
- 이후 새 `mansu-*` 스킬을 만들 때는 먼저 source skill map을 작성하고, Mansu-owned boundary를 따로 적는다.

## 2026-04-24: Align `mansu-debug-rootcause` with Mansu philosophy
> **"디버그 스킬도 만능 디버거가 아니라 root-cause 규율을 지키는 오케스트레이터다."**

### 1. Work summary
- `mansu-debug-rootcause`의 `Source relationship`을 `Source skill map`으로 바꾸었다.
- Oh My `debug`, gstack `investigate`, `build-fix`, `doctor`, `git-master`, `browse/qa-only`, `qa`, `benchmark`, `trace`, `team`을 언제 읽거나 호출할지 표로 정리했다.
- `Mansu-owned boundary`를 추가해서 Mansu가 직접 책임질 영역과 source skill에 위임할 영역을 분리했다.
- 핵심 원칙을 `thin where it can be thin and strict where it must be strict`로 고정했다.

### 2. Verification
- `scripts/validate_mansu_debug_rootcause.sh`가 source skill map과 Mansu-owned boundary를 검사하도록 업데이트했다.

### 3. Follow-up
- 실제 디버그 작업에서 source skill map이 너무 넓거나 좁은지 관찰하고, 자주 반복되는 분기는 별도 `mansu-debug-*` 스킬로 분리한다.

## 2026-04-24: Add root README
> **"Mansu는 좋은 source skill을 더 안전하게 조합하기 위한 운영 레이어다."**

### 1. Work summary
- `mansu-skills` 루트 README를 추가했다.
- 상단에서 Mansu의 정체성, 설치, agent용 시작 프롬프트를 바로 볼 수 있게 했다.
- `mansu-*` 스킬 목록, 추천 라우팅, source skill policy, 검증 명령어, repo map을 정리했다.
- README가 `MANSU_PHILOSOPHY.md`와 같은 철학을 공유하도록 맞췄다.

### 2. Verification
- 전체 Mansu skill validation과 markdown diff check를 실행한다.

### 3. Follow-up
- 원격 `main`을 canonical로 쓸지, 현재 `codex/*` branch를 리뷰 branch로 유지할지 결정한다.

## 2026-04-24: Strengthen `mansu-tdd-total` planning gate
> **"계획은 예시가 아니라 게이트다. 구현은 accepted plan과 slice table 뒤에만 온다."**

### 1. Work summary
- `mansu-tdd-total`의 planning loop를 강화했다.
- non-trivial work에서 단일 TODO식 계획을 금지하고, Planner / Critics / Synthesizer 역할을 필수로 매핑하게 했다.
- 정확한 Prometheus, Metis, Momus 또는 gstack 도구명이 없더라도 같은 책임을 맡는 도구/관점을 `PLAN.md`에 기록하게 했다.
- accepted plan 요구사항에 goal, non-goals, DDD-style boundary, architecture/module boundary, source skills, risks, validation strategy, docs/worklog target, approval status를 추가했다.
- README에도 multi-role critique loop를 Mansu 핵심 철학으로 노출했다.

### 2. Verification
- `scripts/validate_mansu_tdd_total.sh`가 required planning roles와 accepted plan requirements를 검사하도록 강화됐다.

### 3. Follow-up
- 실제 기능 작업에서 `PLAN.md` 템플릿이 필요해지면 `mansu-tdd-total`에 accepted plan template을 추가할지 검토한다.

## 2026-04-24: Promote `mansu-skills` to main
> **"완성된 Mansu 스킬은 작업 브랜치가 아니라 main을 기준으로 공유한다."**

### 1. Work summary
- 완성된 `codex/mansu-tdd-lite-hardening` 상태에서 `main` 브랜치를 만들었다.
- README에 canonical branch가 `main`임을 명시했다.
- `codex/*` 브랜치는 임시 작업 브랜치이고, 완료된 스킬 변경은 `main`에 반영한다는 branch policy를 추가했다.

### 2. Verification
- 전체 Mansu skill validation과 `git diff --check`를 실행한다.

### 3. Follow-up
- 필요하면 GitHub repository default branch도 `main`으로 맞춘다.

## 2026-04-24: Strengthen sequential slice gate
> **"다음 slice는 이전 slice가 닫힌 뒤에만 시작한다."**

### 1. Work summary
- `mansu-tdd-total`에 `Sequential slice gate`를 추가했다.
- slice N+1은 slice N이 validation, review, QA, checkpoint, `PLAN.md`, worklog, commit/no-commit reason까지 닫힌 뒤에만 시작하게 했다.
- `mansu-tdd-lite`에도 current lite slice close 전 다음 slice 금지 문구를 명시했다.
- README에도 sequential slice 기본 원칙을 노출했다.

### 2. Verification
- `scripts/validate_mansu_tdd_total.sh`와 `scripts/validate_mansu_tdd_lite.sh`가 sequential slice gate 문구를 검사하도록 강화됐다.

### 3. Follow-up
- 실제 작업에서 병렬 slice가 필요하면 dependency-free order를 사용자가 명시 승인했을 때만 허용한다.

## 2026-04-25: Reframe planning gate as automatic execution readiness
> **"계획은 사람 승인 대기실이 아니라, 자동 실행해도 안전한 상태를 만드는 품질 게이트다."**

### 1. Work summary
- `mansu-tdd-total`의 `accepted plan` 표현을 `execution-ready plan`으로 바꾸었다.
- non-trivial work에서 planning role, critique, slice table이 끝나면 자동 실행하는 것이 기본임을 명시했다.
- 사람 승인으로 멈추는 경우를 `명시적 승인 게이트`, `위험 작업`, `미해결 blocker`로 제한했다.
- README와 `MANSU_PHILOSOPHY.md`에도 같은 철학을 반영했다.
- `mansu-tdd-lite`와 `mansu-tdd-strict`의 handoff 표현도 execution-ready plan 기준으로 맞췄다.

### 2. Verification
- `scripts/validate_mansu_tdd_total.sh`가 execution-ready plan과 자동 실행 게이트를 검사하도록 업데이트했다.

### 3. Follow-up
- 실제 작업 보고서에는 `실제 multi-agent 사용 여부`와 `역할 시뮬레이션 여부`를 명시하게 할지 검토한다.

## 2026-04-27: Add real critic agent wait gate
> **"자동 실행은 critic을 무시한다는 뜻이 아니다."**

### 1. Work summary
- `mansu-tdd-total`에 `Real critic agent gate`를 추가했다.
- 실제 critic sub-agent나 외부 critic tool을 시작한 경우, 그 결과를 planning gate 입력으로 취급하게 했다.
- critic이 pending이면 execution-ready plan을 잠그거나 구현을 시작하지 못하게 했다.
- critic 실패/timeout fallback은 `PLAN.md`에 기록하고, 고위험 미해결 질문은 `blocked`로 남기게 했다.
- README와 `MANSU_PHILOSOPHY.md`에도 같은 원칙을 반영했다.

### 2. Verification
- `scripts/validate_mansu_tdd_total.sh`가 real critic agent gate 문구를 검사하도록 업데이트했다.

### 3. Follow-up
- 실제 작업 보고서에 critic 이름, 상태, fallback 여부를 항상 포함시키는 템플릿을 추가할지 검토한다.

## 2026-04-27: Add final validation and report closeout
> **"작업은 구현이 아니라 검증, 커밋, 남은 리스크 보고까지 끝나야 닫힌다."**

### 1. Work summary
- `mansu-tdd-total` closeout에 project-level build/test/type/lint suite 실행을 추가했다.
- 사용자-visible 변경이 있으면 final QA 또는 browser verification을 하게 했다.
- 최종 검증 뒤 commit하거나, commit을 미룬 이유를 기록하게 했다.
- 최종 보고에 검증한 것, 검증하지 못한 것, 남은 리스크, 후속 확인 사항을 반드시 포함하게 했다.
- `mansu-tdd-lite`와 strict gate reference에도 같은 closeout 원칙을 반영했다.

### 2. Verification
- `scripts/validate_mansu_tdd_total.sh`와 `scripts/validate_mansu_tdd_lite.sh`가 final validation/report closeout 문구를 검사하도록 업데이트했다.

### 3. Follow-up
- 실제 프로젝트별로 `build/test/type/lint` 명령이 다를 수 있으니, 각 repo의 PLAN.md나 README에 권장 최종 검증 명령을 남기는 방식을 검토한다.

## 2026-05-02: Add `mansu-start`
> **"좋은 스킬도 처음 설치와 업데이트가 흔들리면 계속 불안해진다."**

### 1. Work summary
- `mansu-start`를 새 Mansu entry skill로 추가했다.
- 처음 설치, 업데이트, 복구, 상태 확인을 `check/install/update/repair` 모드로 나누었다.
- gstack, Oh My Codex / OMC, `$HOME/.codex/skills`, Mansu repo 상태를 함께 확인하게 했다.
- 최신 gstack의 `context-save` / `context-restore` 전환을 compatibility check에 포함했다.
- README Quick Start를 `mansu-start` 부트스트랩 흐름으로 바꾸었다.

### 2. Verification
- `scripts/validate_mansu_start.sh`를 추가하고 전체 `scripts/validate_mansu_skills.sh`에 연결한다.

### 3. Follow-up
- 실제 새 환경에서 `mansu-start install`을 돌려보고, 설치 리포트가 충분한지 다듬는다.

## 2026-05-02: Port Mansu toward Hermes + OpenCode runtime
> **"좋은 운영 철학은 유지하고, 실행 결합부만 현재 런타임 기준으로 다시 배선한다."**

### 1. Work summary
- `README.md` Quick Start를 Codex-first 진입점에서 runtime-target-first 진입점으로 바꿨다.
- `mansu-start`를 Hermes/OpenCode/tmux 현실에 맞게 다시 썼다.
  - runtime target을 먼저 고르고
  - skill target directory를 정한 뒤
  - `omx` / `omo` / `omc`는 optional compatibility tooling으로 다루게 했다.
- `mansu-debug-rootcause`에서 `$HOME/.codex/skills/...` 경로 하드코딩을 제거하고, runtime-local 또는 installed equivalent source skill 개념으로 바꿨다.
- `mansu-tdd-total`은 Planner / Critics / Synthesizer 역할 discipline은 유지하되, Hermes + OpenCode session / tmux-backed helper session 기준으로 읽히게 했다.
- `mansu-tdd-strict`는 `Oh My XX`, `gstack-review`, `gstack-qa`, `gstack-checkpoint` 같은 역사적 고정명을 걷어내고, current-runtime orchestration + callable `review` / `qa` / `checkpoint` gate family 기준으로 바꿨다.
- `docs/runtime-target-matrix.md`를 추가해 runtime target, skill target, adapter expectation, fallback behavior를 별도 문서로 정리했다.
- 관련 validator들도 함께 수정해, touched area에서 stale terminology가 다시 들어오면 잡히게 했다.

### 2. Verification
- `scripts/validate_mansu_start.sh` PASS
- `scripts/validate_mansu_debug_rootcause.sh` PASS
- `scripts/validate_mansu_tdd_total.sh` PASS
- `scripts/validate_mansu_tdd_series.sh` PASS
- `scripts/validate_mansu_skills.sh` PASS
- targeted search로 `mansu-debug-rootcause`와 `mansu-tdd-strict`에서 오래된 Codex path / strict runtime terminology 제거를 확인했다.

### 3. Remaining follow-up
- broader runtime-readiness validator를 추가해 suite 전체 stale path/tool-name drift를 한 번에 잡을지 결정한다.
- `mansu-web-verify`와 남은 문서들에 아직 older runtime mental model drift가 있는지 후속 점검한다.
- Hermes의 canonical skill target path를 완전히 고정할지, host-detected wording을 유지할지 결정한다.

## 2026-05-02: Add suite-level runtime-readiness validator
> **"개별 스킬만 고치면 다시 샌다. 이제는 스위트 전체에서 오래된 런타임 가정을 잡아야 한다."**

### 1. Work summary
- `scripts/validate_mansu_runtime_readiness.sh`를 추가했다.
- 새 validator는 runtime-facing 파일 집합을 명시하고, 그 범위에서 high-signal stale 패턴을 검사한다.
- top-level `scripts/validate_mansu_skills.sh`에 새 validator를 연결했다.
- `README.md` Validation 섹션에 `scripts/validate_mansu_runtime_readiness.sh`를 노출했다.
- 검사 관점은 다음처럼 제한했다:
  - runtime-first / adapter-optional 계약이 canonical docs에 남아 있는지
  - host-specific skill target path가 허용된 runtime-target zone 밖으로 새지 않는지
  - legacy OpenCode home path가 runtime-facing 문서에 다시 들어오지 않는지
  - `Oh My XX`, `gstack-review`, `gstack-qa`, `gstack-checkpoint` 같은 오래된 고정명이 허용된 zone 밖으로 새지 않는지
  - adapter wording이 시작/런타임 타깃 문맥 밖으로 과도하게 퍼지지 않는지
- validator는 deliberate allowlist 기반이라, planning/worklog 같은 historical 기록 영역까지는 일부러 뒤지지 않게 했다.

### 2. Verification
- `scripts/validate_mansu_runtime_readiness.sh` PASS
- `scripts/validate_mansu_skills.sh` PASS
- `git diff --check` PASS

### 3. Remaining follow-up
- 현재 validator는 high-signal pattern check 중심이며 semantic validator는 아니다.
- `mansu-web-verify`와 남은 문서들에 아직 older runtime mental model drift가 있는지 별도 점검할 수 있다.
- Hermes canonical skill-target path를 완전히 고정할지 여부는 아직 결정 전이다.

## 2026-05-02: Add document creation router
> **"문서는 많을수록 좋은 게 아니라, 다음 결정을 덜 흔들리게 할 때만 좋다."**

### 1. Work summary
- `mansu-operating-model/references/DOCUMENT_CREATION_ORDER.md`를 추가했다.
- 문서별 생성 순서, 작성에 쓸 source skill, 검증에 쓸 source skill, exit criteria를 표로 정리했다.
- `PROJECT_BLUEPRINT.md` 같은 고정 파일명을 표준처럼 강제하지 않고, gstack design doc, `.codex/plans`, `docs/ideas`, spec, Implementation Plan, ADR 같은 원천 스킬의 실제 산출물 관례를 먼저 따르게 했다.
- 다만 "프로젝트 관례를 따른다"만으로는 찾기 어렵기 때문에, 강제 규칙이 아니라 `Locator Reference Map`으로 먼저 찾을 위치와 없을 때의 suggested fallback path를 명시했다.
- `PLAN.md`는 전체 기억 저장소가 아니라 현재 phase/slice 실행 문서라는 경계를 다시 명시했다.
- `README.md`, `mansu-operating-model/SKILL.md`, `SOURCE_SKILL_CATALOG.md`에서 새 document router를 찾을 수 있게 연결했다.

### 2. Verification
- `scripts/validate_mansu_operating_model.sh`가 `DOCUMENT_CREATION_ORDER.md`의 핵심 섹션과 source skill 연결을 검사하도록 업데이트했다.
- `scripts/validate_mansu_doc_system.sh`가 README와 canonical title 연결을 검사하도록 업데이트했다.

### 3. Remaining follow-up
- 실제 새 프로젝트에 적용할 때, 어떤 문서가 과하고 어떤 문서가 충분히 가벼운지 관찰해서 matrix를 다듬는다.

## 2026-05-03: Add hidden source curator
> **"만수는 원천 스킬을 베끼지 않는다. 대신 계속 확인하고, 바뀐 지도를 다시 그린다."**

### 1. Work summary
- `mansu-source-curator`를 내부 관리 스킬로 추가했다.
- 이 스킬은 gstack, Oh My / OMO / OMC, addyosmani/agent-skills, runtime adapter를 업데이트하거나 확인한 뒤 Mansu reference docs, validators, worklog, local installed skills를 같이 맞추는 역할이다.
- `mansu-start`에는 일반 설치/업데이트 진입점 역할을 유지하되, source-reference drift가 생기면 `mansu-source-curator`로 넘기도록 연결했다.
- `SOURCE_SKILL_CATALOG.md`에는 source refresh and reference curation recipe를 추가했다.

### 2. Verification
- `scripts/validate_mansu_source_curator.sh`를 추가했다.
- suite-level validator가 새 curator 검증도 함께 돌도록 연결했다.

### 3. Remaining follow-up
- 실제 gstack / Oh My / addyosmani source update가 발생했을 때 `full-refresh` 모드 리포트가 충분한지 관찰한다.

## 2026-05-03: Multiagent review follow-up for zero-to-release flow
> **"개별 스킬이 좋아도 처음부터 끝까지 이어지는 길이 없으면 운영체계가 아니다."**

### 1. Work summary
- 멀티에이전트 검증에서 나온 누락을 반영했다.
- `mansu-project-start`를 추가해 새 프로젝트/큰 기능군을 idea, research, spec/TDR, architecture, UI direction, phase `PLAN.md`까지 끌고 가는 first-class route로 만들었다.
- `mansu-ship-release`를 추가해 구현 이후 health, review, QA, docs, PR, deploy/canary, rollback, learning closeout 기준으로 ship/hold를 판단하게 했다.
- `SOURCE_SKILL_LOCK.json`을 추가해 gstack, Oh My runtime, addyosmani/agent-skills source family freshness를 기록할 machine-readable snapshot을 만들었다.
- `mansu-start update`의 순서를 source curation과 validation 이후 runtime sync로 바꿔, 검증 전 스킬이 설치되는 위험을 줄였다.
- `CODING_RULES.md` template에 Gate Mapping과 Learning Closeout을 추가했다.
- `mansu-tdd-total/lite/strict`에는 equivalent gate의 최소 기준과 upstream artifact traceability를 보강했다.

### 2. Verification
- `scripts/validate_mansu_project_start.sh` PASS
- `scripts/validate_mansu_ship_release.sh` PASS
- `scripts/validate_mansu_source_lock.sh` PASS
- `scripts/validate_mansu_operating_model.sh` PASS
- `scripts/validate_mansu_doc_system.sh` PASS
- `scripts/validate_mansu_tdd_series.sh` PASS

### 3. Remaining follow-up
- 실제 greenfield 프로젝트에서 `mansu-project-start`가 너무 무겁거나 가볍지 않은지 확인한다.
- 실제 release flow에서 `mansu-ship-release`가 ship/hold 판단과 learning closeout을 충분히 잘 남기는지 확인한다.

## 2026-05-07: Park bkit as a future idea, not an active source

### 1. Work summary
- bkit은 지금 당장 도입하지 않기로 했다.
- 활성 source routing map인 `SOURCE_SKILL_CATALOG.md`와 machine-readable source lock인 `SOURCE_SKILL_LOCK.json`에는 bkit을 넣지 않기로 정리했다.
- 대신 `mansu-operating-model/references/FUTURE_BKIT_IDEAS.md`를 새로 만들었다.
- 이 문서는 나중에 bkit식 아이디어로 Mansu 스킬을 개발할 때 볼 parking-lot 문서다.
- 보존한 미래 아이디어는 gap check, docs as state, bounded iteration, phase transition criteria, PDCA/state-machine framing이다.
- 가져오지 않을 패턴은 automation levels, mandatory Claude plugin dependency, whole state-machine copy로 명시했다.
- Mansu는 level 기반 절충이 아니라 maximum autonomy를 지향한다는 점을 다시 고정했다.

### 2. Verification
- `scripts/validate_mansu_doc_system.sh`
- `scripts/validate_mansu_source_lock.sh`
- `scripts/validate_mansu_operating_model.sh`
- `scripts/validate_mansu_skills.sh`
- `git diff --check`

### 3. Remaining follow-up
- bkit을 실제 도입 후보로 다시 볼 때는 public Codex-specific install contract가 있는지 먼저 확인한다.
- `mansu-ship-release`에서 gap-check가 실제 ship/hold 판단에 충분히 도움이 되는지 관찰한다.

## 2026-05-08: Split Ouroboros setup from project start
> **"`mansu-setting`은 준비하고, `mansu-project-start`는 시작한다."**

### 1. Work summary
- `mansu-setting`의 역할을 Ouroboros, gstack, 현재 runtime의 Oh My adapter를 준비/설치/업데이트하는 bootstrap gate로 명확히 했다.
- `mansu-setting`은 `ooo auto`, `ooo interview` 같은 프로젝트 생성 명령을 실행하지 않고, runtime setup과 버전/설치 상태 확인까지만 맡도록 제한했다.
- `mansu-project-start`에는 Ouroboros PM/interview/Seed/Ledger 흐름을 프로젝트 정의 엔진으로 사용하는 route를 추가했다.
- Ouroboros 산출물은 Mansu가 그대로 실행하는 대상이 아니라, research/spec/TDR/UI 방향과 phase `PLAN.md`로 변환해야 하는 upstream source-of-truth artifact로 정의했다.
- `mansu-source-curator`, `SOURCE_SKILL_CATALOG.md`, `SOURCE_SKILL_LOCK.json`에도 Ouroboros를 active source family로 추가했다.

### 2. Verification
- `scripts/validate_mansu_setting.sh`
- `scripts/validate_mansu_project_start.sh`
- `scripts/validate_mansu_source_curator.sh`
- `scripts/validate_mansu_source_lock.sh`
- `scripts/validate_mansu_operating_model.sh`
- `scripts/validate_mansu_skills.sh`
- `git diff --check`

### 3. Remaining follow-up
- 실제 greenfield 프로젝트에서 `mansu-setting install` 후 `mansu-project-start`가 Ouroboros Seed/Ledger를 너무 과하지 않게 잘 받아먹는지 확인한다.
- Ouroboros upstream이 바뀌면 `mansu-source-curator`로 lock/catalog/validator를 같이 갱신한다.

## 2026-05-08: Define mansu-project-start persona
> **"프로젝트 시작 스킬은 질문봇이 아니라, 초안을 만드는 PM이어야 한다."**

### 1. Work summary
- `mansu-project-start`에 Persona 섹션을 추가했다.
- 기본 페르소나를 project-start PM / producer로 정의했다.
- 먼저 이해한 프로젝트를 working draft로 만들고, 방향을 크게 바꾸는 질문만 1~3개씩 묻도록 했다.
- 답이 없어도 막히지 않는 부분은 명시적 가정으로 진행하고, 위험하거나 큰 결정만 사용자에게 확인하도록 했다.
- README와 OpenAI metadata prompt에도 같은 행동 양식을 반영했다.

### 2. Verification
- `scripts/validate_mansu_project_start.sh`
- `scripts/validate_mansu_skills.sh`
- `git diff --check`

### 3. Remaining follow-up
- 실제 프로젝트 시작 대화에서 질문 수가 과하거나 부족하지 않은지 관찰한다.

## 2026-05-08: Thin down project-start persona into orchestration
> **"Mansu는 Ouroboros를 흉내 내지 않는다. 불러오고, 받아 적고, 연결한다."**

### 1. Work summary
- 직전 `mansu-project-start` Persona 섹션이 Ouroboros의 PM/interview 역할을 Mansu 안에 너무 많이 하드코딩할 수 있다는 문제를 확인했다.
- `Persona`를 `Orchestration posture`로 바꿨다.
- Mansu는 Ouroboros project interview / PM / Seed / Ledger logic을 재구현하지 않고, 가능하면 Ouroboros에 위임하도록 명시했다.
- Mansu가 직접 책임지는 것은 중복 질문 방지, 중요한 결정만 사용자에게 확인, source artifact를 Mansu 문서/phase/gate/PLAN으로 변환하는 일로 좁혔다.
- README와 validator도 같은 방향으로 바꿔서, 다시 두꺼운 인터뷰어 스킬로 회귀하지 않게 했다.

### 2. Verification
- `scripts/validate_mansu_project_start.sh`
- `scripts/validate_mansu_skills.sh`
- `git diff --check`

### 3. Remaining follow-up
- 실제 `mansu-project-start` 사용 시 Ouroboros가 없는 fallback 경로가 너무 적극적으로 질문하지 않는지 확인한다.

## 2026-05-08: Add Ouroboros route selector to project-start
> **"Mansu의 일은 프로젝트를 대신 인터뷰하는 것이 아니라, 지금 쓸 Ouroboros route를 고르는 것이다."**

### 1. Work summary
- Ouroboros 원본 `skills/*/SKILL.md` 목록을 다시 확인했다.
- `mansu-project-start`에 `Ouroboros route selector`를 추가했다.
- 상황별로 `setup/welcome/help`, `brownfield`, `interview`, `pm`, `seed`, `auto --skip-run`, `run`, `status/resume-session`, `evaluate/qa`, `unstuck`, `evolve/ralph`, `publish` 중 어떤 route를 쓸지 정리했다.
- `SOURCE_SKILL_CATALOG.md`의 Ouroboros Capability Map도 같은 route selector 기준으로 확장했다.
- `SOURCE_SKILL_LOCK.json`에 project-start에서 참고하는 Ouroboros skills를 더 명확히 기록했다.

### 2. Verification
- `scripts/validate_mansu_project_start.sh`
- `scripts/validate_mansu_operating_model.sh`
- `scripts/validate_mansu_source_lock.sh`
- `scripts/validate_mansu_skills.sh`
- `git diff --check`

### 3. Remaining follow-up
- 실제 프로젝트에서 `interview`와 `pm` 중 무엇을 먼저 고를지 판단 기준이 충분한지 확인한다.

## 2026-05-08: Generalize project-start route selector
> **"Mansu는 특정 도구의 라우터가 아니라, 상황에 맞는 source route를 고르는 오케스트레이터다."**

### 1. Work summary
- `mansu-project-start`의 `Ouroboros route selector` 표현이 특정 도구에 과하게 고정되어 보일 수 있음을 확인했다.
- 섹션을 `Project-start route selector`로 일반화했다.
- Ouroboros는 강한 기본 route 후보로 유지하되, gstack, Oh My, addyosmani, project-local docs까지 함께 고르는 source route selector로 바꿨다.
- `Ouroboros project-start route` 별도 섹션은 제거하고, 선택 기준을 공통 route selector에 통합했다.
- README와 OpenAI metadata prompt도 “어떤 source route를 쓸지 고른다”는 표현으로 수정했다.

### 2. Verification
- `scripts/validate_mansu_project_start.sh`
- `scripts/validate_mansu_skills.sh`
- `git diff --check`

### 3. Remaining follow-up
- 실제 프로젝트 시작에서 Ouroboros를 우선해야 할 때와 gstack/addyo/Oh My를 우선해야 할 때의 기준을 더 관찰한다.

## 2026-05-08: Add Mansu stack model
> **"Mansu는 만능 스킬이 아니라, 좋은 source route들을 이어 붙이는 프로젝트 운영체계다."**

### 1. Work summary
- 사용자가 제안한 큰 레이어 그림을 Mansu의 핵심 철학으로 반영했다.
- `MANSU_PHILOSOPHY.md`에 `Mansu stack model`을 추가했다.
- README 상단에 Big Picture 섹션을 추가해 Project memory/lifecycle, Mansu, execution mode, specialist team, engineering order, implementation actor를 한눈에 볼 수 있게 했다.
- `SOURCE_SKILL_CATALOG.md`의 Layer Model에도 같은 구조를 반영했다.
- Ouroboros, Oh My, gstack, addyosmani, Codex는 대표 route이며 고정 의존성이 아니라는 점을 명시했다.

### 2. Verification
- `scripts/validate_mansu_doc_system.sh`
- `scripts/validate_mansu_operating_model.sh`
- `scripts/validate_mansu_skills.sh`
- `git diff --check`

### 3. Remaining follow-up
- 실제 운영에서 특정 source route가 더 나은 대체재로 바뀌면, 이름이 아니라 layer 역할 기준으로 교체한다.

## 2026-05-09: Add mansu-help for beginners
> **"초보자에게 필요한 첫 기능은 더 많은 규칙이 아니라, 지금 어디인지 알려주는 지도다."**

### 1. Work summary
- `mansu-help`를 새 스킬로 추가했다.
- 역할은 실행이 아니라 상황 진단, route 추천, 다음에 쓸 copy-paste prompt 제공이다.
- 초보자가 불안하거나 모호할 때는 read-only diagnosis를 기본으로 두도록 했다.
- `mansu-setting`, `mansu-project-start`, `mansu-operating-model`, `mansu-tdd-total`, `mansu-debug-rootcause`, `mansu-web-verify`, `mansu-ship-release`로 이어지는 route selector를 넣었다.
- README, OpenAI metadata, suite validator에 `mansu-help`를 연결했다.

### 2. Verification
- `scripts/validate_mansu_help.sh`
- `scripts/validate_mansu_skills.sh`
- `git diff --check`

### 3. Remaining follow-up
- 실제 초보자 질문에서 추천 route가 너무 길거나 부담스럽지 않은지 관찰한다.

## 2026-05-09: Beginner self-test for mansu-help
> **"초보자에게는 완전한 지도보다, 지금 한 걸음이 먼저다."**

### 1. Work summary
- `mansu-help`를 초보자 입장에서 읽고 시뮬레이션했다.
- 기본 구조는 맞았지만, 첫 응답에서 `source route`, `gate`, `slice` 같은 내부 용어가 먼저 나오면 어렵게 느껴질 수 있음을 확인했다.
- 첫 응답 규칙을 추가했다: 상황 한 문장, 안전한 다음 행동 한 문장, 그 다음 정확한 `mansu-*` route와 복붙 프롬프트.
- `Beginner shortcuts`를 추가해 "처음인데 뭐 해야 해?", "새 프로젝트 시작하고 싶어", "기능 만들고 싶어", "에러가 나" 같은 흐릿한 말도 바로 route로 연결되게 했다.

### 2. Verification
- `scripts/validate_mansu_help.sh`
- `scripts/validate_mansu_skills.sh`
- `git diff --check`

### 3. Remaining follow-up
- 실제 사용자 대화에서 `mansu-help`가 너무 짧아 답답한지, 너무 길어 부담스러운지 관찰한다.

## 2026-05-09: Add static mansu-manual HTML
> **"매번 생성하지 말고, 초보자가 바로 열 수 있는 매뉴얼을 유지한다."**

### 1. Work summary
- `mansu-manual` 스킬을 추가했다.
- 처음에는 생성 스크립트 방식으로 생각했지만, 사용자의 방향에 맞춰 정적 HTML 문서를 유지하는 방식으로 바꿨다.
- `docs/mansu-manual.html`을 추가해 초보자가 브라우저에서 Mansu 큰 그림, skill index, 시작 경로, 복붙 프롬프트, 용어 사전을 볼 수 있게 했다.
- 새 `mansu-*` 스킬을 추가하거나 이름/목적이 바뀌면 HTML 매뉴얼도 같은 변경에서 업데이트하도록 `mansu-manual`과 validator에 명시했다.
- README와 suite validator에 `mansu-manual`을 연결했다.

### 2. Verification
- `scripts/validate_mansu_manual.sh`
- `scripts/validate_mansu_skills.sh`
- `git diff --check`

### 3. Remaining follow-up
- 실제 브라우저에서 읽었을 때 초보자에게 너무 길거나 짧지 않은지 확인한다.

## 2026-05-09: Rework manual around development lifecycle
> **"초보자 매뉴얼은 스킬 목록보다, 지금 개발 여정의 어디에 있는지를 먼저 보여줘야 한다."**

### 1. Work summary
- `docs/mansu-manual.html`의 기본 화면을 스킬 카드 나열 중심에서 개발 생애주기 중심으로 바꿨다.
- `개발 생애주기`와 `아키텍처 / 큰그림` 탭을 추가했다.
- 큰그림/아키텍처 설명은 기본 화면에서 빼고, 궁금한 사람만 보는 별도 탭으로 옮겼다.
- 용어사전 섹션을 제거했다.
- 생애주기에는 도움/설정/프로젝트 시작/운영 원칙/구현 계획/slice 실행/디버그/웹 검증/출하/관리 단계와 각 단계의 프롬프트를 넣었다.

### 2. Verification
- `scripts/validate_mansu_manual.sh`
- `scripts/validate_mansu_skills.sh`
- `git diff --check`

### 3. Remaining follow-up
- 실제 브라우저에서 탭 전환과 모바일 레이아웃이 읽기 좋은지 확인한다.

## 2026-05-09: Polish manual design and hide strict/lite choice
> **"초보자는 strict/lite를 고르는 사람이 아니라, mansu-tdd-total에게 맡기는 사람이다."**

### 1. Work summary
- 매뉴얼 디자인을 더 강한 제품 매뉴얼 톤으로 다시 잡았다.
- 기존 카드 나열 느낌을 줄이고, sticky tab, numbered lifecycle, command panel, richer background를 적용했다.
- 생애주기에서 `상황에 맞게 strict 또는 lite로 구현한다` 단계를 제거했다.
- 구현 단계는 `mansu-tdd-total` 하나만 노출하고, strict/lite는 Mansu가 내부에서 판단한다고 설명했다.
- strict/lite는 하단 Skill Index에만 “내부에서 선택될 수 있는 세부 모드”로 남겼다.

### 2. Verification
- `scripts/validate_mansu_manual.sh`
- `scripts/validate_mansu_skills.sh`
- `git diff --check`

### 3. Remaining follow-up
- in-app browser에서 새로고침 후 실제 시각 밀도와 모바일 폭을 확인한다.

## 2026-05-10: Add awesome-design-md as design reference source
> **"디자인 레퍼런스는 실행 스킬이 아니라, 우리 DESIGN.md를 더 잘 쓰기 위한 원천이다."**

### 1. Work summary
- `VoltAgent/awesome-design-md`를 Mansu의 디자인 레퍼런스 패밀리로 등록했다.
- 이 레퍼런스는 runtime skill directory에 설치하는 대상이 아니라, UI 작업에서 `DESIGN.md`를 만들거나 갱신할 때 참고하는 원천으로 정의했다.
- `SOURCE_SKILL_CATALOG.md`, `DOCUMENT_CREATION_ORDER.md`, `mansu-project-start`, `mansu-setting`, `mansu-source-curator`, README, HTML 매뉴얼에 디자인 레퍼런스 역할을 연결했다.
- `DESIGN.md`의 핵심 기준으로 visual atmosphere, color roles, typography, component styling, layout, depth/elevation, do/don't, responsive behavior, agent prompt guide를 명시했다.
- source lock에 `/tmp/awesome-design-md` snapshot `beec066`, `DESIGN.md` inventory 71개를 기록했다.
- 멀티에이전트 검증 후 `DOCUMENT_CREATION_ORDER.md`, `mansu-project-start`, OpenAI metadata, HTML 매뉴얼에 reference-only guardrail을 더 강하게 넣었다.
- 매뉴얼의 빈 quick-find scaffold를 제거하고, source lock에는 README badge count와 local file count가 다를 수 있음을 명시했다.

### 2. Verification
- `scripts/validate_mansu_source_lock.sh`
- `scripts/validate_mansu_source_curator.sh`
- `scripts/validate_mansu_setting.sh`
- `scripts/validate_mansu_operating_model.sh`
- `scripts/validate_mansu_manual.sh`
- `scripts/validate_mansu_project_start.sh`
- `scripts/validate_mansu_doc_system.sh`
- `scripts/validate_mansu_skills.sh`
- `git diff --check`

### 3. Remaining follow-up
- 실제 UI 프로젝트에서 `DESIGN.md`가 너무 브랜드 복제처럼 흐르지 않고, 프로젝트 고유의 디자인 기준으로 변환되는지 관찰한다.
