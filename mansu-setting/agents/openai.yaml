interface:
  display_name: "Mansu Setting"
  short_description: "Runtime-aware Mansu bootstrap and updater"
  default_prompt: "Use $mansu-setting to detect the runtime target, agent host, and OS, install or update Ouroboros, gstack, and the matching Oh My adapter when needed, read/verify the Mansu manual agent guide and catalog, choose the right skill directory, sync Mansu skills, verify Ouroboros, gstack, Oh My / OMO / OMC, and addyosmani/agent-skills source freshness, hand off source-reference drift to $mansu-source-curator, report optional adapter compatibility, run validation, and report skipped checks or compatibility drift."
