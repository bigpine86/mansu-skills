interface:
  display_name: "Mansu Setting"
  short_description: "Runtime-aware Mansu bootstrap and updater"
  default_prompt: "Use $mansu-setting to detect the runtime target, agent host, and OS, install or update Ouroboros, gstack, and the matching Oh My adapter when explicitly needed, read/verify the Mansu manual agent guide and catalog, choose the right skill directory, verify Ouroboros, gstack, Oh My / OMO / OMC, addyosmani/agent-skills, and VoltAgent/awesome-design-md source freshness in read-only source-check mode by default, sync Mansu skills only after approved update/install/repair, hand off source-reference drift to $mansu-source-curator, report optional adapter compatibility, run validation, and report skipped checks or compatibility drift."
