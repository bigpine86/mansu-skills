interface:
  display_name: "Mansu Start"
  short_description: "Host-aware Mansu bootstrap and updater"
  default_prompt: "Use $mansu-start to detect the agent host and OS, choose the right skill directory, sync Mansu skills, verify gstack and the matching Oh My adapter, refresh source skill links when needed, run validation, and report compatibility drift."
