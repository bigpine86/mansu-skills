interface:
  display_name: "Mansu Start"
  short_description: "Runtime-aware Mansu bootstrap and updater"
  default_prompt: "Use $mansu-start to detect the runtime target, agent host, and OS, choose the right skill directory, sync Mansu skills, verify gstack, Oh My / OMO / OMC, and addyosmani/agent-skills source freshness, hand off source-reference drift to $mansu-source-curator, report optional adapter compatibility, run validation, and report skipped checks or compatibility drift."
