interface:
  display_name: Mansu 1 Define
  short_description: Define what to build before planning.
  default_prompt: Use $mansu-1define as the primary numbered route backed by mansu-define. Read mansu-define/SKILL.md, treat short user idea notes as seed context for Ouroboros interview rather than a completed definition, complete the definition gate, and if complete tell the user to use mansu-2plan next.
