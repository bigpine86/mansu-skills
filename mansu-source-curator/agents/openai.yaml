interface:
  display_name: "Mansu Source Curator"
  short_description: "Internal source-skill freshness curator"
  default_prompt: "Use $mansu-source-curator to check or update Ouroboros, gstack, Oh My / OMO / OMC, addyosmani/agent-skills, VoltAgent/awesome-design-md, and adapter source freshness. Default to read-only check; update Mansu references, validators, worklog, the HTML manual agent guide/catalog, dry-run routing, and local installed skills only when the user explicitly approved mutation and validation passes."
