interface:
  display_name: "Mansu Source Curator"
  short_description: "Internal source-skill freshness curator"
  default_prompt: "Use $mansu-source-curator to check or update Ouroboros, gstack, Oh My / OMO / OMC, addyosmani/agent-skills, VoltAgent/awesome-design-md, Open Design, and adapter source freshness. Default to read-only check; update Mansu references, validators, worklog, the HTML manual agent guide/catalog, dry-run routing, and local installed skills only when the user explicitly approved mutation and validation passes. For Open Design, preserve it as an external callable design route and have Mansu run approved CLI/lifecycle commands directly rather than asking the user to copy them."
