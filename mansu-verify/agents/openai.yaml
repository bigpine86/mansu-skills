interface:
  display_name: Mansu Verify
  short_description: Prove behavior with tests, runtime checks, browser QA, and evidence.
  default_prompt: Use $mansu-verify to prove behavior with tests, runtime checks, browser QA, and QA evidence before review or ship.
